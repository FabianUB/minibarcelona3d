# =============================================================================
# Production Environment Variables for MiniBarcelona3D
#
# Copy to .env and fill in values:
#   cp .env.prod.example .env
#
# IMPORTANT: Never commit the .env file to version control!
# =============================================================================

# Domain name (required)
# Caddy will automatically obtain SSL certificates
DOMAIN=minibarcelona3d.com

# Mapbox token (required)
# Get one at: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# TMB API credentials (required for Metro/Bus data)
# Register at: https://developer.tmb.cat/
TMB_APP_ID=your_tmb_app_id
TMB_APP_KEY=your_tmb_app_key

# =============================================================================
# Optional Configuration (defaults shown)
# =============================================================================

# POLL_INTERVAL=30        # Seconds between real-time polls
# RETENTION_HOURS=1       # Hours to keep historical data
# STATIC_REFRESH_DAYS=7   # Days between GTFS static data refresh
