# =============================================================================
# Production Environment Variables for MiniBarcelona3D
#
# Copy this file to .env and fill in the values:
#   cp .env.prod.example .env
#   nano .env
#
# IMPORTANT: Never commit the .env file to version control!
# =============================================================================

# =============================================================================
# DOMAIN CONFIGURATION
# =============================================================================

# Your domain name (without https://)
# Caddy will automatically obtain SSL certificates for this domain
DOMAIN=minibarcelona3d.example.com

# =============================================================================
# MAPBOX (Required)
# =============================================================================

# Mapbox access token for map rendering
# Get one at: https://account.mapbox.com/access-tokens/
# Required scopes: styles:read, fonts:read, sprites:read, tiles:read
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# =============================================================================
# TMB API (Required for Metro/Bus data)
# =============================================================================

# TMB API credentials for Barcelona Metro/Bus real-time data
# Register at: https://developer.tmb.cat/
TMB_APP_ID=your_tmb_app_id
TMB_APP_KEY=your_tmb_app_key

# =============================================================================
# POLLING CONFIGURATION (Optional - defaults shown)
# =============================================================================

# How often to poll for real-time vehicle positions (seconds)
# POLL_INTERVAL=30

# How long to keep historical position data (hours)
# RETENTION_HOURS=1

# How often to refresh static GTFS data (days)
# STATIC_REFRESH_DAYS=7

# =============================================================================
# NOTES
# =============================================================================
#
# After configuring, deploy with:
#   docker compose -f docker-compose.prod.yml up -d --build
#
# View logs:
#   docker compose -f docker-compose.prod.yml logs -f
#
# Restart services:
#   docker compose -f docker-compose.prod.yml restart
#
# Stop everything:
#   docker compose -f docker-compose.prod.yml down
#
# =============================================================================
