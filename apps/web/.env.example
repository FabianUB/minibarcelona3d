# Mapbox access token used by the frontend when rendering the Rodalies map.
# Obtain a token from https://account.mapbox.com/ and ensure it has Styles:Read scope.
VITE_MAPBOX_TOKEN=pk.your-mapbox-token-here

# Directory where bundled Rodalies GeoJSON assets live relative to public/.
# These assets should mirror the structure defined in specs/001-show-rodalies-map/research.md.
RODALIES_DATA_DIR=public/rodalies_data
